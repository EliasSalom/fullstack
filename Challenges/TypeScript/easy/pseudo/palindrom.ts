export {};
//Have the function Palindrome(str) take the str parameter being passed and return the string true if the parameter is a palindrome,
//(the string is the same forward as it is backward) otherwise return the string false.
//For example: "racecar" is also "racecar" backwards.
//Punctuation and numbers will not be part of the string.

function palindrome() {
    // 1. Declare a variable for reversed and trimmed version of the string. (Remove all spaces)
    // 2. Compare the reversed and the only-trimmed version of the string. if they are equal return true, else false.
}
