export {};
function factorial(num) {
    // 1. If the number is 1 or smaller, return it.
    // 2. If the number is greater than 1 continue to the next step
    // 3. Take the number minus one of himself and power it by the number itself
}
