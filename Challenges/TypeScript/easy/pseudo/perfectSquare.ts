export {};
/*
Have the function PowersofTwo(num) take the num parameter being passed which will be an integer and return the string true if it's a power of two. 
If it's not return the string false. For example if the input is 16 then your program should return the string true 
but if the input is 22 then the output should be the string false.
 */

function perfectSquare(num) {
    // 1. Set counter as 1
    // 2. Multiply that counter by 2 with a while loop until you pass your number.
    // 3. Check if the counter equal to the num
}
