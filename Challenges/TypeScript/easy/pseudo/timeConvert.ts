export {};
//Have the function TimeConvert(num) take the num parameter being passed and return the number of hours and minutes the parameter converts to
//(ie. if num = 63 then the output should be 1:3).
//Separate the number of hours and minutes with a colon.

function timeConvert(num) {
    // 1. Declare hours and minutes variables, and initialize them to zero.
    // 2. Loop trough the num size.
    // 3. Add in any loop siecle 1 to the minute variable.
    // 4. If the minutes variable reached to 60, add 1 to the hours variable and reset the minutes variable.
    // 5. Return the result in a template literals form
}
