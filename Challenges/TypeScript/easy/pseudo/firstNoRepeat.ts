export {};
/* 
Have the function NonrepeatingCharacter(str) take the str parameter being passed, which will contain only alphabetic characters and spaces, 
and return the first non-repeating character. For example: if str is "agettkgaeee" then your program should return k. 
The string will always contain at least one character and there will always be at least one non-repeating character.
*/

function nonRepeatingCharacter(str) {
    // 1. Convert the string into an array and remove all the spaces using regex.
    // 2. Declare an object to collect the data.
    // 3. Loop trough the array and add to the object the key as the letter and a counter if needed.
    // 4. Return using find method the first non repeated letter.
}
