export {};
//Have the function SimpleAdding(num) add up all the numbers from 1 to num.
//For example: if the input is 4 then your program should return 10 because 1 + 2 + 3 + 4 = 10.
//For the test cases, the parameter num will be any number from 1 to 1000.
function simpleAdd(num) {
    // 1. Declare a counter variable to get the index of the operation
    // 2. Declare a result variable to collect the added number. initialize it to 1
    // 3. Create a while loop => While the counter is less than the given number do this: add 1 to the counter, then add the counter to result.
    // 4. Return the result
}
