export {};
/*
Have the function LargestPair(num) take the num parameter being passed and determine the largest double digit number within the whole number. 
For example: if num is 4759472 then your program should return 94 because that is the largest double digit number. 
The input will always contain at least two positive digits.
*/

function largestPair(num) {
    // 1. Change the number into an array of strings.
    // 2. Declare an initial value for the largest number with zero.
    // 3. Loop trough the array and check if the current item plus the next item in a string form is larger than the largest number in integer form.
    // 4. Set the largest number to that number if true.
    // 5. Return the result
}
