export {};
function reverseString(str) {
    // 1. Take the string and split it by any character in it. (Note: space is also a character)
    // 2. Reverse the given array from the previous step.
    // 3. Join the characters without any parameter but string concatenation.
}
